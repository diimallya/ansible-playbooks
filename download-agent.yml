--- 
  - name: Download the agent 
    hosts: all
    gather_facts: false
    tasks: 
      - name: Download the monitoring agent
        get_url:
          url: "{{ src_image  }}"
          dest: "{{ src_images_dir }}"
          mode: 0777
          force: yes